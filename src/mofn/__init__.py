"""m-of-n — key-centric semantic assertions.

Intentionally empty. ARCH-0001 is at v0.1.0 draft with DEC-001..DEC-005 and
DEC-007 open: no encoding, tag language, or envelope has been selected. WP1
(artifacts/schema/, encoding-neutral) lands before any of this is written, so
that reduction can be built against the logical model rather than a guess.

Implementation begins in increment I2 (T-030). Adding code before then would
quietly decide DEC-002 by accident, which ARCH-0001 §9 exists to prevent.
"""
__version__ = "0.0.0"
